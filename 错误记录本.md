# 错误记录本

## 案例 1：主题模板应用错误（2025-12-01）

### 问题背景
用户要求将两个前端模块统一到共享主题模板 `@packages/pip-boy-theme`。

### 错误过程

**1. 理解偏差**
- 错误理解：主题模板是"替代品"，应该用模板的类名替换原有代码
- 正确理解：主题模板是"增强工具"，应该在保留原有功能的基础上引入

**2. 执行错误**
- 删除了原有的 CSS 变量定义（`@theme`、`:root`）
- 删除了原有的 CRT 效果实现
- 用模板类名（如 `pip-boy-container`）替换原有类名（如 `crt-container`）
- 用模板颜色类（如 `text-pip-boy-green`）替换原有颜色类（如 `text-pip-green`）

**3. 导致的问题**
- 页面布局完全失效
- 文字颜色不可见
- 滚动功能丢失
- 原有功能全部损坏

**4. 后续遗漏问题**

即使恢复了原有代码，仍出现以下问题：

| 问题 | 原因 | 修正 |
|-----|------|-----|
| 扫描线效果缺失 | 恢复时遗漏了 `::after` 伪元素和 `@keyframes scanline` | 完整复制原模块的 CRT 效果代码 |
| 文字颜色偏暗 | 缺少 `text-shadow` 发光效果 | 在 body 添加 `text-shadow: 0 0 5px rgba(74, 246, 38, 0.5)` |
| 缺少统一头部 | 新模块没有参考原模块的 ROBCO 头部结构 | 复制 log-viewer 的 header 结构 |
| 标题字体不一致 | 全局 `h1` 样式覆盖了 Tailwind 类 | 移除全局 `h1` 样式，仅保留 `h2, h3` |

**根本原因**：恢复代码时没有逐项对比原模块的完整实现，导致遗漏关键细节。

### 修正方法

1. 通过 `git show HEAD:文件路径` 查看原始代码
2. 完整恢复原有样式和组件代码
3. 仅在 CSS 文件顶部添加 `@plugin` 和 `@config` 引入主题模板
4. 保留原有类名和变量，模板功能作为可选增强

### 核心教训

| 错误做法 | 正确做法 |
|---------|---------|
| 替换原有代码 | 在原有代码基础上增强 |
| 假设模板能完全覆盖需求 | 先验证原有功能，再逐步引入模板 |
| 批量修改多个文件 | 小步修改，每步验证 |
| 只看模板文档 | 先理解现有代码的实现方式 |
| 恢复代码时凭记忆 | 逐行对比原模块的完整实现 |
| 全局样式不考虑优先级 | 检查 CSS 优先级冲突，避免覆盖 Tailwind 类 |

---

## 建议 Prompt

在执行类似"统一风格"、"应用模板"、"重构样式"等任务时，使用以下 prompt：

```
在执行此任务前，请遵循以下原则：

1. **先理解后执行**
   - 先用 git show 查看原始代码的完整实现
   - 理解原有代码的功能、类名、变量命名
   - 确认哪些是核心功能，哪些是可替换的

2. **增强而非替换**
   - 模板/库是增强工具，不是替代品
   - 保留原有的功能实现
   - 仅添加模板的引入语句
   - 模板提供的类名作为可选项，不强制使用

3. **最小化修改**
   - 每次只修改一个文件
   - 修改后立即验证功能是否正常
   - 如果出错，立即回滚而非继续修改

4. **验证清单**
   - [ ] 原有功能是否保留？
   - [ ] 页面布局是否正常？
   - [ ] 颜色/字体是否可见？
   - [ ] 交互功能是否正常？

5. **多模块一致性检查**
   - 新模块必须逐项对比参考模块的实现
   - 检查：CRT 效果、发光效果、头部结构、字体大小
   - 检查：全局 CSS 是否覆盖了 Tailwind 类
```

---

## 适用场景

- 引入新的 UI 框架或主题
- 统一多个模块的样式
- 重构 CSS/样式代码
- 迁移到新的组件库

---

## 案例 2：提示词优化时错误删除功能字段（2025-12-02）

### 问题背景
用户发现 AI 回复中包含了思考过程（如括号注释），要求优化提示词禁止这种行为。

### 错误过程

**1. 任务理解正确**
- 需要在提示词中添加"禁止在 response 中包含括号注释"的约束
- 这是一个简单的约束添加任务

**2. 执行时过度优化**
- 在修改输出格式说明时，为了"简化 JSON 结构、减少出错概率"，删除了多个功能字段
- 删除的字段：

| 提示词 | 删除的字段 |
|--------|-----------|
| GROW_REALITY_PROMPT | 将 reality_elements 压缩为单行 |
| GROW_OPTIONS_PROMPT | evaluation_criteria |
| GROW_WAY_FORWARD_PROMPT | contingency, support |
| SUMMARY_PROMPT | action_items |
| ENHANCED_SUMMARY_PROMPT | timeline_insights, cross_domain_patterns |

**3. 导致的问题**
- `contingency` 和 `evaluation_criteria` 在 `grow-handler.ts` 中被使用
- 删除这些字段会导致代码逻辑无法正确提取数据
- 功能丢失但不会立即报错，属于隐性 bug

**4. 问题发现**
- 用户通过查看 git diff 发现了字段被删除
- 用户主动询问"是否有些文件为了减少出错而舍弃了一些功能"

### 根本原因

| 错误思维 | 正确思维 |
|---------|---------|
| "简化 JSON 能减少 AI 出错" | 功能字段是代码依赖的，不能随意删除 |
| 只关注当前修改目标 | 需要检查字段是否被代码使用 |
| 假设字段是"示例" | 提示词中的字段是与代码约定的接口 |

### 修正方法

1. 使用 `grep` 检查被删除字段是否在代码中被引用
2. 恢复所有被删除的功能字段
3. 只保留核心修改：添加"禁止括号注释"的约束

### 核心教训

| 错误做法 | 正确做法 |
|---------|---------|
| 顺手"优化"不相关的内容 | 严格限制修改范围，只做被要求的事 |
| 假设字段是可选的 | 修改前用 grep 检查字段是否被代码使用 |
| 简化 JSON 结构 | 保持与代码的接口约定不变 |
| 批量修改多个提示词 | 修改一个验证一个 |

### 检查清单

修改提示词/接口定义时：
- [ ] 是否只修改了必要的部分？
- [ ] 删除/修改的字段是否被代码引用？（用 grep 检查）
- [ ] 修改后的结构是否与代码解析逻辑匹配？
- [ ] 是否有"顺手优化"的冲动？如果有，停下来确认是否必要

---

## 建议 Prompt

在执行"修改提示词"、"优化输出格式"、"调整接口定义"等任务时，使用以下 prompt：

```
在执行此任务前，请遵循以下原则：

1. **严格限定修改范围**
   - 只修改用户明确要求的部分
   - 不要"顺手"优化其他内容
   - 不要为了"简化"而删除字段

2. **提示词字段 = 代码接口**
   - 提示词中的 JSON 字段是与代码约定的接口
   - 修改前必须用 grep 检查字段是否被代码使用
   - 示例：`grep -r "contingency" --include="*.ts"`

3. **禁止假设字段可选**
   - 不要假设某个字段是"示例"或"可选"
   - 除非代码中有明确的默认值处理，否则字段必须保留
   - JSON 结构的"简化"往往意味着功能丢失

4. **修改前检查**
   - [ ] 列出即将删除/修改的字段
   - [ ] 用 grep 检查每个字段是否被代码引用
   - [ ] 如果被引用，保留该字段

5. **修改后验证**
   - [ ] 对比修改前后的 JSON 结构
   - [ ] 确认只有必要的修改，没有额外的"优化"
   - [ ] 确认代码依赖的字段全部保留
```

---

## 适用场景

- 修改提示词模板
- 修改 API 接口定义
- 修改配置文件结构
- 任何涉及"接口约定"的修改

---

**记录人**: Claude
**审核人**: 壮爸
**更新日期**: 2025-12-02
